DFS Recursive

Reviewed by Linda 13 July 2025

Animation:  

At some point we might want to do something more graphical for the stack.

Pseudocode line 17, I think this should be double == 
            if DFS1(G, m, n) == FOUND , rather than the current  if DFS1(G, m, n) = FOUND 


Background good.